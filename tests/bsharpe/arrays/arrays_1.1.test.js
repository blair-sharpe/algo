const assert = require("assert");
const problem = require("../../../solutions/bsharpe/arrays/arrays_1.1.js");

context("arrays 1.1", function () {
  it("should do something", function () {
    assert(problem(), true);
  });
  it("should do something", function () {
    assert(true, true);
  });
  it("should do something", function () {
    assert(true, true);
  });
});
